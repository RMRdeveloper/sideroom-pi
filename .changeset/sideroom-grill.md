---
"@rmrdeveloper/sideroom-pi": minor
---

Add `sideroom-grill` skill: interview fuzzy plans in `sideroom_ask` rounds and write the settled vocabulary to `CONTEXT.md` and hard decisions to `docs/adr/`
