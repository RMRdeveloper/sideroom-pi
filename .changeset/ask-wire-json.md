---
"@rmrdeveloper/sideroom-pi": patch
---

Decode JSON-string `sideroom_ask` questions and nested options before the strict schema check so OpenCode-style tool calls still validate question structure.
