---
"@rmrdeveloper/sideroom-pi": minor
---

Add `sideroom-domain-modeling` skill: sharpen the ubiquitous language while designing, write settled terms to `CONTEXT.md` with `_Avoid_` synonyms, and offer ADRs only when all three gates pass
